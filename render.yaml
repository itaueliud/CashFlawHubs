services:
  - type: web
    name: CashFlawHubs-backend
    env: node
    plan: starter
    repo: itaueliud/CashFlawHubs
    branch: main
    rootDir: backend
    buildCommand: npm install
    startCommand: npm run start
    healthCheckPath: /health
    autoDeploy: true
